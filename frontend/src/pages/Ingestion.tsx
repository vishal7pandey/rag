import React from 'react';

const Ingestion: React.FC = () => {
  return (
    <section aria-labelledby="ingestion-heading">
      <h2 id="ingestion-heading">Ingestion</h2>
      <p>Upload and manage documents for the RAG system.</p>
    </section>
  );
};

export default Ingestion;
